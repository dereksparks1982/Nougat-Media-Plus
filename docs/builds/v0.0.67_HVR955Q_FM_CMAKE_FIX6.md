# Nougat Play Portal v0.0.67 HVR-955Q FM CMake Fix 6

This follow-up resumes after REBOOT_SAFE_FIX5 successfully installed the two reboot-safe kernel modules on disk but failed during application configuration because `PROJECT/build/CMakeCache.txt` was created under the former `Nougat Play Portal` source path.

Fix 6 does not repeat the kernel source download/build and does not unload or reload the running HVR/media stack. It verifies the already-installed `cx231xx.ko` and `si2157.ko` match the running kernel, then patches/builds the Nougat application in the dedicated clean directory:

`/home/dereksparks1982/DKLab/Projects/Nougat Play Portal/build/app-v67`

The legacy top-level `build/CMakeCache.txt` is never reused, and the kernel build material under `build/v67-hvr955q-fm-*` is left untouched.

Git and GitHub remain untouched.
