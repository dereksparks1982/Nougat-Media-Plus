# Nougat Media Suite v0.0.38 Validation

## Candidate scope

v0.0.38 is the same-version repair/polish candidate built from accepted v0.0.37. The approved component sheet remains the literal UI authority and no unrelated layout movement is authorized.

- Library Search typed text/caret are visible in the existing Search row.
- Continue Watching admits local movies/episodes after 10 seconds of actual playback.
- Library cards prioritize titles and move technical details to stitched hover/focus information.
- The loading/progress bar stays in its original system-wide lane directly below the top tabs, uses the literal approved-sheet PROGRESS BAR, is tall enough to contain the percent, and carries the percent inside the moving caramel fill.
- Seek keeps its exact approved-sheet stitched artwork. Only the separate pale underside crop-shadow is omitted.
- VOLUME keeps its current approved geometry/artwork. Only the small pale underside crop-shadow is omitted and the percentage is black.
- Existing mouse-wheel scrolling is preserved. Visible approved-sheet-style vertical scrollbars are present on vertically scrollable surfaces that lacked them.
- Live TV opens on the guide grid and `Guide` is the single guide-view button; the redundant `Channels` control is removed.
- Only `Detect Tuner` and `Refresh Tuner` move from Live TV to System.
- Real locally packaged broadcaster/network logos are used when identified; unmatched stations use call-sign text, never a duplicate numbered badge.
- Remembered/keyboard tuning, current-program identity, and aligned broadcast timing remain active.
- Accepted ATSC Watch Live playback/pause behavior is preserved.
- Installer accepted-baseline hashing remains fail-closed.
- Diagnostic interpretation redesign remains v0.0.39 work.

## Automated gates

The installer runs protected license/Search checks, the v0.0.38 source contract, installer rollback contract, retained P2P/diagnostics tests, warnings-as-errors stub compilation, retained security-runtime validation, full native libtorrent + llama.cpp compilation, RPATH verification, historical executable self-tests, the v0.0.38 self-test, root-executable validation, and launcher installation.

## Owner-visible acceptance

The owner must visually/hardware-check exact approved-sheet fidelity, original loading-bar placement, contained percentage behavior, seek/volume underside-shadow removal without artwork loss, black volume percentage, visible scrollbars with unchanged mouse wheel, Live TV Guide-only navigation, System tuner controls, real channel logos, current-program overlay/timing, and real ATSC playback.

- Final repair: Live TV logo coverage includes Busted and ION Plus; rounded logo cards clip artwork to the approved rounded slot.
