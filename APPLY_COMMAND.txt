Nougat Media Suite v0.0.38 repaired changed-files candidate

Package:
Nougat_Media_Suite_v0_0_38_REPAIRED_CANDIDATE_2_CHANGED_FILES_ONLY.zip

Apply from Ubuntu Terminal:
cd "$HOME/Downloads"
package="Nougat_Media_Suite_v0_0_38_REPAIRED_CANDIDATE_2_CHANGED_FILES_ONLY.zip"
candidate="$HOME/Downloads/Nougat_Media_Suite_v0_0_38_REPAIR_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$candidate"
unzip "$package" -d "$candidate"
bash "$candidate/INSTALL_NOUGAT_MEDIA_SUITE_v0_0_38.sh"

The installer accepts only the exact accepted v0.0.37 touched baseline or the exact known rejected v0.0.38 repair base. It snapshots touched state, validates package hashes fail-closed, builds with warnings as errors, runs retained regression gates, performs the full native build, and atomically replaces the root Nougat_Media_Suite_v38 executable only after validation passes.
