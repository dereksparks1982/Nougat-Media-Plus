Nougat Media Suite v0.0.37 changed-files build

Download the ZIP to $HOME/Downloads, extract it into a fresh folder, then run:

  bash INSTALL_NOUGAT_MEDIA_SUITE_v0_0_37.sh

The installer verifies the exact accepted v0.0.36 touched baseline, preserves known owner archives/generated runtimes, snapshots touched state for rollback, runs warnings-as-errors and native builds, then copies Nougat_Media_Suite_v37 to the project root only after validation passes.
